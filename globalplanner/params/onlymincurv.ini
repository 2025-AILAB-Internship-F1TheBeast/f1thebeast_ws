# ----------------------------------------------------------------------------------------------------------------------
# MINIMUM CURVATURE OPTIMIZATION CONFIGURATION FILE
# This file contains only the parameters required for minimum curvature global path generation
# ----------------------------------------------------------------------------------------------------------------------

[GENERAL_OPTIONS]

# Basic optimization parameters
opt_params = {"stepsize": 0.1, "max_iterations": 200, "convergence_threshold": 1e-8, "alpha_max": 10.0}

# Vehicle parameters required for optimization
veh_params = {"curvlim": 0.12}

# ----------------------------------------------------------------------------------------------------------------------
[OPTIMIZATION_OPTIONS]
#나중에 도로 보고 생각해보기 아래 형식으로 들어가 있음
#double max_left = std::min(track_widths[i].left - 0.2, optim_opts_.width_opt / 2);
# Minimum curvature optimization options 
optim_opts_mincurv={"width_opt": 3.0}

# ----------------------------------------------------------------------------------------------------------------------
[GRADIENT_DESCENT_OPTIONS]

# Gradient descent specific parameters
gd_params = {"learning_rate": 0.1, "eps": 0.01, "penalty_factor": 0.9}

# ----------------------------------------------------------------------------------------------------------------------
[QP_SOLVER_OPTIONS]

# QP solver specific parameters
qp_params = {"curvature_weight": 1.0, "regularization_weight": 0.1, "centerline_penalty": 0.1, "learning_rate": 0.02, "tolerance": 1e-6, "max_qp_iterations": 1000, "random_init_range": 0.1}
